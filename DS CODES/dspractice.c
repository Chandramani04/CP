// pratice all the operation on linkedlist in c

#include <stdio.h>
#include <stdlib.h>

// practicing stack in c
struct node {
    int data;
    struct node *next;
};

struct node *front = NULL, *rear = NULL;

// enqueue -> insert element in front 
// dequeue -> delete element from rear


int main() {
    return 0;
}